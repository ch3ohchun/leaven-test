---
description: 自動生成專案的基礎資料夾架構 (backend/ 和 frontend/)、設定檔及必要的啟動檔案。
---

1. 建立以下目錄結構：
   - `backend/services`
   - `backend/test`
   - `backend/config`
   - `backend/data`
   - `frontend/public`
   - `frontend/src/components`
   - `frontend/src/pages`
   - `frontend/src/assets`
   - `frontend/src/utils`

2. 建立 `backend/main.py` (空白或範例內容)。
3. 建立 `frontend/src/App.jsx` (或類似進入點)。
4. 確保符合專案架構 (Project Structure) 規範。
5. 建立.github/PULL_REQUEST_TEMPLATE.md
6. uv做版本控制(uv init, uv venv, uv add)