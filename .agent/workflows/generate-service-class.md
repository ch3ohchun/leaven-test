---
description: 根據輸入的後端功能主題，自動生成一個帶有主入口方法的 Python 服務類別檔案，並遵循後端的大駝峰式命名法。
---
1. 確定目標目錄 (通常在 `backend/services` 下的某個模組內)。
2. 生成 Python 檔案。
3. 定義 Class，使用大駝峰式命名 (PascalCase)。
4. 定義一個公開的主入口方法 (snake_case)。
5. 定義必要的私有方法 (以 `_` 開頭)。
